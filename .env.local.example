# Transfer Radar — ortam değişkenleri
# Bu dosyayı '.env.local' olarak kopyala ve anahtarını gir:
#   cp .env.local.example .env.local
# Sonra: npm run dev   (http://localhost:3000)
#
# Anthropic API anahtarı (console.anthropic.com'dan alınır).
# NOT: Claude Pro aboneliği API'yi kapsamaz; API kullanımı ayrı ücretlidir.
ANTHROPIC_API_KEY=
